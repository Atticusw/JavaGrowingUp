package com.atticusw.eeaew.oriented.clazz;

public class Util {
    // 类中的接口，作用可以看作是对类功能的进一步补充，类里面包含两部分：
    // 一部分是自己的固定的，一部分是可以变化的，而这可变的部分就编程了一个接口。
    public interface Worker{
        void woke();
    }
}
